/build/
/out/
/dist/
/src/
/downloads/
*.deb
vmlinuz
Image
*.dtb
lib/modules/
.config
.config.old

# Do not exclude the directory itself, otherwise the negation below has no
# effect: git never descends into an excluded directory to re-include files.
/kernel/*
!/kernel/*.sha256

/build/
/out/
/src/
/downloads/
*.deb
vmlinuz
Image
*.dtb
lib/modules/
.config
.config.old

# Каталог не исключаем целиком, иначе отрицание ниже не сработает:
# git не возвращает файлы из исключённого каталога.
/kernel/*
!/kernel/*.sha256
